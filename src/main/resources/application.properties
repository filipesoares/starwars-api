# LOGGING
logging.file=starwars-api.log
logging.level.org.springframework=INFO

server.port=9000
server.servlet.context-path=/starwars/api/v1

# JACKSON (JacksonProperties)
spring.jackson.serialization.fail-on-empty-beans=false
